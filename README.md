# CTF exercises

Authentication and cryptography challenge sources. These exercises deliberately contain weaknesses and are not production authentication examples.

CI compiles the Python sources. Five utility tests cover byte order, round trips, empty input, and XOR boundaries. Run `python -m unittest discover -s tests`. Authentication behavior is not covered. The Bedrock policy file is a separate saved policy example.
